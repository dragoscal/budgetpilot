{
  "name": "budgetpilot-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute budgetpilot-db --local --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
